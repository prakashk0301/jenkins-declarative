pipeline   //comment: declarative pipeline alwasy starts with pipeline
{
    stages  //it contains all the stages
   {
      stage('print a message')  //stage name
      { 
        steps { echo 'hello_jenkins'  }   //it tells jenkins what to do
      }


    }
}